################################################################################
# Deno Edge CDN - env.example
# - このファイルはサンプルです。実際のシークレットは .env に置くか、
#   Deno Deploy / GitHub Secrets 等の安全な場所に登録してください。
################################################################################

# --- Basic origin config ---
# 単一オリジンを使う場合（もっとも簡単）
ORIGIN=https://origin.example.com

# ORIGINS を使うとホストごとにオリジンをマッピングできます（JSON文字列）。
# 例: ORIGINS='{"example.com":"https://origin.example.com","api.example.com":"https://api-origin.example.com"}'
# 注意: JSON はシェルや環境によりエスケープが必要な場合があります。
ORIGINS=''

# --- Purge / 管理 ---
# /_purge エンドポイントでの認証トークン。ランダムで長めの文字列推奨。
PURGE_TOKEN=replace-with-a-strong-random-token

# --- Cache settings ---
# Edge 側の cache 名前（複数プロジェクトを同じ環境で分けたい時に変更）
CACHE_NAME=edge-cdn-cache

# デフォルトのキャッシュ TTL (秒) をアプリ側で使う場合のフォールバック
DEFAULT_MAX_AGE=60

# --- Deno KV / Prefix purge ---
# プレフィックス一括パージを使うなら true
# （Deno Deploy が KV をサポートしている環境で有効にしてください）
USE_DENO_KV=false

# --- Optional toggles ---
# 開発向けに詳細ログを出すかどうか（true/false）
ENABLE_DEBUG=false

# --- Rate limiting / safety (任意) ---
# パージの1分当たり最大リクエスト数（アプリ側で実装する場合）
PURGE_RATE_LIMIT_PER_MIN=30

# --- Extra / Advanced ---
# 追加ヘッダを常時付与する場合などの設定（任意・自由フォーマット）
EXTRA_HEADERS=''

################################################################################
# 注意:
# - このファイルはサンプルです。シークレット（PURGE_TOKEN 等）は絶対に
#   Git 管理下に公開しないでください。Deno Deploy の「Environment
#   Variables」画面か GitHub Secrets に登録してください。
# - ORIGINS はシェルで扱う際にクォートやエスケープに注意してください。
################################################################################
